# Leave empty for local dev (Vite proxy handles /api -> localhost:5000)
# In production (Vercel/Netlify), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
